Q.1 write a c program to check whether a number is divisible by 97 or not.
CODE:
#include<stdio.h>
#include<math.h>

int main(){
    int n;
  printf("Enter the check its divisiblity by 97:");
  scanf("%d", &n);

  if(n%97==0)
    {
    printf("Number is divisible by 97\n");
    }
  else
    {
    printf("Number isnt divisible by 97\n");
    }
    return 0;
}



Q.2 write a c program  to find whether a year entered by the user is a leap year or not. Take years as  an input from the user.
CODE:

#include<stdio.h>
#include<math.h>

int main(){
    int year;
    printf("Enter year: ");
    scanf("%d",&year);

    if(year%4==0)
    {
    printf("%d is a leap year\n", year);
    }
   else if(year%100==0)
   {
    printf("%d is not a leap year\n", year);
    }
    else if(year%400==0)
    {
    printf("%d is a leap year\n", year);
    }
    else{
        printf("%d is not a leap year",year);
    }
    return 0;
}



Q.3 Write a c program  to find whether a character entered by the user is lower case or not.
CODE:
#include<stdio.h>

int main(){
   char ch;

   printf("Enter a character:");
   scanf("%c", &ch);

if(ch>='A'  &&  ch<='Z')
{
    printf("'%c' is a Uppecase Character", ch);
}
else if(ch>='a'  &&  ch<='z')
{
    printf("'%c' is a Lowercase Character", ch);
}
else
{
    printf("'%c'is not a character", ch);
}
return 0;
}



Q.4 Write a c program  to find gratest of four numbers entered by the user.

#include<stdio.h>
int main()
{
    int n1,n2,n3,n4,n5;
    printf("Enter the Four numbers:");
    scanf("%d %d %d %d", &n1,&n2,&n3,&n4);
    if(n1>n2)
    {
        if(n1>n3)
        {
              if(n1>n4)
              {
                   printf("n1=%d is the greatest number", n1);
              }
               
        }
    }
    else
    {
        if(n2>n3)
        {
            if(n2>n4)
            {
                printf("n2=%d is the greatest number", n2);
            }
            
        }
    	else 
    	{
        	if(n3>n4)
       		 {
           	 printf("n3=%d is the greatest number", n3);
       		 }
       		 else
        	 {
             	 printf("n4=%d is the greatest number", n4);
        	 }
         }
    }
return 0;
}

